namespace RealityToolkit.webrtc.Definitions
{
    public struct Connection
    {
        /*public PeerConnection PeerConnection;
        public SharingSignaler Signaler;
        public DataChannel DataChannel;
        
        public Connection(PeerConnection peerConnection, SharingSignaler sharingSignaler, DataChannel dataChannel = null)
        {
            PeerConnection = peerConnection;
            Signaler = sharingSignaler;
            DataChannel = dataChannel;
        }*/
    }
}